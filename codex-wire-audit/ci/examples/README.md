# CI examples

These workflows are templates, not active workflows in this bundle. Copy them into a repository's `.github/workflows/` directory after setting a reviewed pinned SHA and committing a baseline report.

The current-main canary should upload its semantic diff even when it fails, so a legitimate upstream protocol change is reviewable rather than opaque.
