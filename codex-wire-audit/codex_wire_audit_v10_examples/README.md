# v10 example output

This example demonstrates the v10 report envelope, source manifest, normalized entities, typed edges, structured rules and diagnostics, and canonical integrity digest. It is a schema seed, not a live repository audit.

Optional source-derived transport profiles, including redirect-generated `Referer`, may be absent from this compact schema seed. Generate against a current Codex checkout to populate them authoritatively.
